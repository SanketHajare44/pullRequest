# DevTinder

UI Lec : 1 ->

- Created a vit + React application started  
- remove unneccesary code and create Helo World App
- install tailwind css -> go to the Documention of Tailwind css
- install DaisyUi - it contains predefined components
- Installed react-router-dom for the define Path like "/home"
- in App.js Create BrowserRoute > Routes> Route "/" render Body.jsx > Route "/login" render Login.jsx
- create outlet in body component
- create footer and set to the bottom 
- create login page 
- install axios in frontend (also mention the credential: true)
- when you re making an API call so pass axois => {withCredentials: true}

- install CORS into the Backend and set the credential as({origin: "http://localhost/3003", credential: true})

- install redux toolkit and react-redux 
- configureStore => Provider => createSlice  = add reducer to store 
- add redux dev tool in chrome
- Login and see if your data is coming properly in the store 
- NavBar should update as soon  as user logs in 
- refoctor our code to add constants file  + create a components folder
- You should not be acccess other routes without login 
- If token is not present , redirect to the login page 
- Logout Feature
- Get the feed and add the feed in the store 
- Buid the user card on feed
- Edit profile Feature 
- show Toast Message on save of profile 

- New Page - see all my connection 
- New Page - see all my connections Requests

    

## RealTime  chat

- Build  the UI  for a  chst window on /chat/:toargetUserId





 